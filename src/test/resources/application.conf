version = "TestVersion"
hostname = "TestHostname"
databaseUrl = "TestDbUrl"

github {
  clientId: "clientId"
  clientSecret: "clientSecret"
  scopes: ["read:user"]
}

jwt {
  expirationTime: "1d"
  algorithm: "HS256"
  secret: "secret"
  privateKey: "private"
  publicKey: "public"
}

auth {
  callbackUrl: "url?t=<<TOKEN>>"
}
